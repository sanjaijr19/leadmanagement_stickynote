# from django.dispatch import Signal
#
# record_created = Signal(providing_args=["user"])
#
# record_updated = Signal(providing_args=["user"])
#
# record_deleted = Signal(providing_args=["user"])
#
#
# signal_record_created = Signal(providing_args=["user"])
#
#
# signal_record_updated = Signal(providing_args=["user"])
#
# signal_record_deleted = Signal(providing_args=["user"])